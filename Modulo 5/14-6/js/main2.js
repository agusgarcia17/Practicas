/*
function fooBar(num){
    var array = [] 
    for (let i = 1; i <= num; i++) {
        array.push(i)
    }
    
    for (let i = 0; i < array.length; i++) {
        var resto = array[i]%3
        var resto1 = array[i]%5

      if (resto == 0 && resto1 !== 0){
          array[i] = 'foo'
      } else if(resto1 == 0 && resto!== 0 ) {
        array[i] = 'bar'
      } else if(resto1 == 0 && resto ==0){
          array[i] = 'fooBar'
        }
    }
    console.log(array)
}

fooBar(15)
*/

function fooBar(num){
    var numeros = [];

    for (let i = 1; i <= num; i++){

    }
}